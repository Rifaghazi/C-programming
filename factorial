//to find the factorial of a given integer.
#include <stdio.h>
int main()
{int i,fact,n;
printf("Enter a number: ");
scanf("%d",&n);
fact=1;
for (i=2;i<=n;i++)
{fact=fact*i;}
printf("Factorial of %d is %d ",n,fact);
return 0;
}
